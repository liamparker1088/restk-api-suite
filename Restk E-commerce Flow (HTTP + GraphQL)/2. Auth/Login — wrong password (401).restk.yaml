# Restk Request
id: "336440315925676037"
name: Login — wrong password (401)
method: POST
url: "{{base_url}}/shop/login"
order: 2
body:
    json: |-
        {
          "username": "qa",
          "password": "nope"
        }
scripts:
    postResponse: "\nnova.test(\"bad password rejected (401)\", function () { nova.expect(nova.response.status).toBe(401); });\nnova.test(\"error is invalid_credentials\", function () { nova.expect(nova.response.json().error).toBe(\"invalid_credentials\"); });\n"
